apiVersion:
kind:
metadata:
  name:
  namespace:
spec:
  replicas:
  template:
    metadata:
      labels:
        app:
    spec:
      containers:
        - name:
          image:
          args:
          ports:
            - containerPorts:
