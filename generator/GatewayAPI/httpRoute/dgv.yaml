Version:
kind:
Name for this kind:
Namespaces that you want this kind to be deployed:
app name to be referenced:
THe name for this parentrefs:
The valide values are(Gateway/Service):
is the network port this Route targets:
name of a section in a Kubernetes resource(invalid values are /):
The namespaces that needs to appear:
When unspecified (gateway.networking.k8s.io) is inferred:
Valid hostnames:
The name for this backend refs:
Weight specifies the proportion of requests forwarded to the referenced backend:
The port number for backendRefs:
The kind for the backendRefs:
The group for the backendRefs:
How to match(Exact/PathPrefix/Regular-expresssion):
The path string value to match:
name of the header to be matched:
Value of HTTP header to match:
specifies how to match against the value of the query parameter(Exact/RegularExpression):
name of HTTP query param to match(exact string match req):
value of HTTP query param to match:
specifies how to match against the value of the query parameter(Exact/RegularExpression):
how to select a HTTP route by matching the HTTP method UPPERCASE(CONNECT/DELETE/GET/HEAD/OPTIONS/PATCH/POST/PUT/TRACE):
Such as (Core/Extended/Implementation-specific/URL-rewrite/ResponseHeaderModifier etc):
defines a schema for a filter that modifies request headers:
defines a schema for a filter that modifies response headers:
Requests are sent to the specified destination:
RequestRedirect defines a schema for a filter that responds to the request with an HTTP redirection:
URLRewrite defines a schema for a filter that modifies a request during forwarding:
implementation-specific extension to the filter behavior:
