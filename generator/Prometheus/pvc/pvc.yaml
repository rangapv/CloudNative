apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-csi-data
  namespace: monitoring
  labels:
    app: prometheus-deployment
spec:
  storageClassName: sc-prom
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 15Gi
