apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-csi2-data
  namespace: monitoring
  labels:
    app: prometheus-deployment
spec:
  storageClassName: slow
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
