apiVersion:
kind:
metadata:
  name:
  namespace:
  labels:
    app:
spec:
  replicas:
  selector:
    matchLabels:
      app:
  strategy:
    rollingUpdate:
      maxSurge:
      maxUnavailable:
    type:
  revisionHistoryLimit:
  minReadySeconds:
  progressDeadlineSeconds:
  template:
    metadata:
      labels:
        app:
    spec:
      nodeSelector:
      nodeName:
      restartPolicy:
      initContainers:
        - name:
          image:
          command:
          volumeMounts:
          - name:
            mountPath:
      containers:
      - name:
        image:
        imagePullPolicy:
        command:
        args:
        ports:
          - containerPort:
            hostIP:
            hostPort:
            name:
            protocol:
        volumeMounts:
          - name:
            mountPath:
            mountPropagation:
            readOnly:
            subPath:
            subPathExpr:
      volumes:
        - name:
          persistentVolumeClaim:
            claimName:
